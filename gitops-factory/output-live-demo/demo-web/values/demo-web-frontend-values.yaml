# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# Source: namespaces/demo-web/demo-web-frontend.yaml
# Re-run the converter to regenerate after the source manifest changes.

configMaps:
  demo-web-frontend-nginx-conf:
    data:
      default.conf: "server {\n  listen 8080;\n  location / {\n    return 200 'hello from demo-web-frontend\\\
        n';\n  }\n}\nserver {\n  listen 8081;\n  location /stub_status {\n    stub_status;\n  }\n}\n"
envFrom:
  shared-ca-bundle:
    type: configMapRef
hpa:
  enabled: true
  maxReplicas: 4
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 70
        type: Utilization
    type: Resource
  minReplicas: 2
image:
  repository: nginx
livenessProbe:
  httpGet:
    path: /
    port: http
  initialDelaySeconds: 10
  periodSeconds: 20
ports:
  admin-http:
    containerPort: 8081
    protocol: TCP
  http:
    containerPort: 8080
    protocol: TCP
readinessProbe:
  httpGet:
    path: /
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10
replicaCount: 2
resources:
  limits:
    cpu: 200m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi
route:
  enabled: true
  targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
routes:
  demo-web-frontend-admin:
    enabled: true
    path: /stub_status
    serviceName: demo-web-frontend-admin
    targetPort: admin-http
service:
  enabled: true
  ports:
    http:
      port: 80
      protocol: TCP
      targetPort: http
  type: ClusterIP
serviceAccount:
  create: true
  name: demo-web-frontend
services:
  demo-web-frontend-admin:
    enabled: true
    ports:
      admin-http:
        port: 8081
        protocol: TCP
        targetPort: admin-http
    selector:
      app: demo-web-frontend
    type: ClusterIP
volumeMounts:
  nginx-conf:
    mountPath: /etc/nginx/conf.d
volumes:
  nginx-conf:
    configMap:
      name: demo-web-frontend-nginx-conf
workload:
  type: deployment

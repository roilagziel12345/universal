# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# Source: namespaces/demo-config/demo-config-ci.yaml
# Re-run the converter to regenerate after the source manifest changes.

configMaps:
  demo-config-shared-settings:
    data:
      ENVIRONMENT: demo
      LOG_LEVEL: info
externalSecrets:
  demo-config-db-credentials:
    data:
    - remoteRef:
        key: secret/data/demo-config/db
        property: password
      secretKey: password
    refreshInterval: 1h
    secretStoreRef:
      kind: SecretStore
      name: demo-config-vault
    target:
      creationPolicy: Owner
extraDeploy:
- "apiVersion: v1\nkind: ServiceAccount\nmetadata:\n  name: demo-config-ci-deployer\n"
rbac:
  roleBindings:
    demo-config-ci-writer-binding:
      roleRef: demo-config-ci-writer
      subjects:
      - kind: ServiceAccount
        name: demo-config-ci
  roles:
    demo-config-ci-writer:
      rules:
      - apiGroups:
        - ''
        resources:
        - configmaps
        verbs:
        - get
        - list
        - watch
        - update
secretStores:
  demo-config-vault:
    provider:
      vault:
        auth:
          kubernetes:
            mountPath: kubernetes
            role: demo-config-ci
            serviceAccountRef:
              name: demo-config-ci
        path: secret
        server: https://vault.example.com:8200
        version: v2
service:
  enabled: false
serviceAccount:
  create: true
  name: demo-config-ci
workload:
  type: none

# Auto-generated MINIMAL override for 'demo-config-ci' — image + literal env only.
# This is the client's single source of truth for day-2 tuning; safe to hand-edit
# and applied LAST (highest precedence) over demo-config-ci-values.yaml.

{}

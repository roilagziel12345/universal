# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# Source: namespaces/demo-data/demo-data-redis.yaml
# Re-run the converter to regenerate after the source manifest changes.

image:
  repository: redis
ports:
  redis:
    containerPort: 6379
readinessProbe:
  initialDelaySeconds: 5
  periodSeconds: 10
  tcpSocket:
    port: redis
replicaCount: 1
resources:
  limits:
    cpu: 200m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi
service:
  enabled: true
  ports:
    redis:
      port: 6379
      targetPort: redis
  type: ClusterIP
workload:
  type: deployment

# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# Source: namespaces/demo-data/demo-data-postgres.yaml
# Re-run the converter to regenerate after the source manifest changes.

env:
  PGDATA:
    value: /var/lib/postgresql/data/pgdata
  POSTGRES_DB:
    value: demo
  POSTGRES_PASSWORD:
    valueFrom:
      secretKeyRef:
        key: password
        name: demo-data-postgres-credentials
  POSTGRES_USER:
    value: demo
image:
  repository: postgres
  tag: 16-alpine
livenessProbe:
  exec:
    command:
    - pg_isready
    - -U
    - demo
  initialDelaySeconds: 30
  periodSeconds: 20
ports:
  postgres:
    containerPort: 5432
readinessProbe:
  exec:
    command:
    - pg_isready
    - -U
    - demo
  initialDelaySeconds: 15
  periodSeconds: 10
replicaCount: 1
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi
secrets:
  demo-data-postgres-credentials:
    stringData:
      password: demo-change-me-123
    type: Opaque
service:
  clusterIP: None
  enabled: true
  ports:
    postgres:
      port: 5432
      targetPort: postgres
  type: ClusterIP
volumeClaimTemplates:
  data:
    accessModes:
    - ReadWriteOnce
    size: 1Gi
volumeMounts:
  data:
    mountPath: /var/lib/postgresql/data
workload:
  type: statefulset

# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# Source: namespaces/identity-svc/identity-svc-alpha.yaml
# Re-run the converter to regenerate after the source manifest changes.

configMaps:
  healthcheck-scripts:
    data:
      healthcheck.sh: '#!/bin/sh

        curl -f http://localhost:8080/health

        '
    labels:
      app.kubernetes.io/name: healthcheck-scripts
  identity-svc-alpha-config:
    data:
      FEATURE_FLAG_BETA: 'true'
      MAX_CONNECTIONS: '100'
    labels:
      app.kubernetes.io/name: identity-svc-alpha-config
dnsPolicy: ClusterFirst
env:
  LOG_LEVEL:
    valueFrom:
      configMapKeyRef:
        key: LOG_LEVEL
        name: logging-defaults
envFrom:
  identity-svc-alpha-secret:
    type: secretRef
image:
  pullPolicy: IfNotPresent
  repository: registry.example.com/identity-svc-alpha
livenessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 15
  periodSeconds: 20
ports:
  http:
    containerPort: 8000
    protocol: TCP
readinessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10
replicaCount: 1
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi
restartPolicy: Always
revisionHistoryLimit: 10
route:
  enabled: true
  targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
schedulerName: default-scheduler
secrets:
  identity-svc-alpha-secret:
    data:
      API_KEY: OTk3NzhmMzFhY2ZmNGE0Njk5NGVmYTlhYjM4NGQ3MDM=
    labels:
      app.kubernetes.io/name: identity-svc-alpha-secret
    type: Opaque
service:
  enabled: true
  labels:
    app.kubernetes.io/name: identity-svc-alpha
  ports:
    http:
      port: 80
      protocol: TCP
      targetPort: http
  type: ClusterIP
serviceAccount:
  automountServiceAccountToken: true
  create: true
  name: identity-svc-alpha-sa
strategy:
  rollingUpdate:
    maxSurge: 25%
    maxUnavailable: 25%
  type: RollingUpdate
terminationGracePeriodSeconds: 30
terminationMessagePath: /dev/termination-log
terminationMessagePolicy: File
workload:
  type: deployment

# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# Source: namespaces/identity-svc/identity-svc-gateway.yaml
# Re-run the converter to regenerate after the source manifest changes.

configMaps:
  identity-svc-gateway-config:
    data:
      FEATURE_ALL: 'true'
    labels:
      app.kubernetes.io/name: identity-svc-gateway-config
dnsPolicy: ClusterFirst
env:
  APP_MODE:
    value: full
envFrom:
  identity-svc-gateway-secret:
    type: secretRef
  logging-defaults:
    type: configMapRef
hpa:
  enabled: true
  maxReplicas: 10
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 70
        type: Utilization
    type: Resource
  minReplicas: 2
image:
  pullPolicy: IfNotPresent
  repository: registry.example.com/identity-svc-gateway
  tag: latest
livenessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 15
  periodSeconds: 20
networkPolicies:
  identity-svc-gateway-net:
    podSelector:
      matchLabels:
        app: identity-svc-gateway
    policyTypes:
    - Ingress
    - Egress
pdb:
  enabled: true
  minAvailable: 1
ports:
  http:
    containerPort: 8080
    protocol: TCP
pvc:
  identity-svc-gateway-data:
    accessModes:
    - ReadWriteOnce
    size: 30Gi
rbac:
  roleBindings:
    identity-svc-gateway-rb:
      roleRef: identity-svc-gateway-role
      subjects:
      - kind: ServiceAccount
        name: identity-svc-gateway-sa
  roles:
    identity-svc-gateway-role:
      rules:
      - apiGroups:
        - ''
        resources:
        - pods
        verbs:
        - get
        - list
readinessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10
replicaCount: 2
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi
restartPolicy: Always
revisionHistoryLimit: 10
route:
  enabled: true
  targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
routes:
  identity-svc-gateway-alt:
    enabled: true
    host: identity-svc-gateway-metrics.apps.example.com
    serviceName: identity-svc-gateway-metrics
    targetPort: http
    tls:
      insecureEdgeTerminationPolicy: Redirect
      termination: edge
schedulerName: default-scheduler
secrets:
  identity-svc-gateway-secret:
    data:
      TOKEN: MjUzMGJhMGYyOWNkNDI3YWE2ZGNiYzRhZGVkNTNmMTY=
    labels:
      app.kubernetes.io/name: identity-svc-gateway-secret
    type: Opaque
service:
  enabled: true
  labels:
    app.kubernetes.io/name: identity-svc-gateway
  ports:
    http:
      port: 80
      protocol: TCP
      targetPort: http
  type: ClusterIP
serviceAccount:
  automountServiceAccountToken: true
  create: true
  name: identity-svc-gateway-sa
services:
  identity-svc-gateway-metrics:
    enabled: true
    labels:
      app.kubernetes.io/name: identity-svc-gateway-metrics
    ports:
      http:
        port: 9090
        protocol: TCP
        targetPort: http
    selector:
      app: identity-svc-gateway
    type: ClusterIP
strategy:
  rollingUpdate:
    maxSurge: 25%
    maxUnavailable: 25%
  type: RollingUpdate
terminationGracePeriodSeconds: 30
terminationMessagePath: /dev/termination-log
terminationMessagePolicy: File
volumeMounts:
  cfg:
    mountPath: /etc/app
  data:
    mountPath: /data
volumes:
  cfg:
    configMap:
      name: identity-svc-gateway-config
  data:
    persistentVolumeClaim:
      claimName: identity-svc-gateway-data
workload:
  type: deployment

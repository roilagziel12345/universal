# Auto-generated MINIMAL override for 'identity-svc-alpha' — image + literal env only.
# This is the client's single source of truth for day-2 tuning; safe to hand-edit
# and applied LAST (highest precedence) over identity-svc-alpha-values.yaml.

env:
  APP_ENV:
    value: staging
image:
  repository: registry.example.com/identity-svc-alpha
  tag: v3.2.0

# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# Source: namespaces/payments-svc/payments-svc-gateway.yaml
# Re-run the converter to regenerate after the source manifest changes.

configMaps:
  payments-svc-gateway-config:
    data:
      FEATURE_ALL: 'true'
    labels:
      app.kubernetes.io/name: payments-svc-gateway-config
dnsPolicy: ClusterFirst
env:
  APP_MODE:
    value: full
envFrom:
  logging-defaults:
    type: configMapRef
  payments-svc-gateway-secret:
    type: secretRef
hpa:
  enabled: true
  maxReplicas: 10
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 70
        type: Utilization
    type: Resource
  minReplicas: 2
image:
  pullPolicy: IfNotPresent
  repository: registry.example.com/payments-svc-gateway
  tag: latest
livenessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 15
  periodSeconds: 20
networkPolicies:
  payments-svc-gateway-net:
    podSelector:
      matchLabels:
        app: payments-svc-gateway
    policyTypes:
    - Ingress
    - Egress
pdb:
  enabled: true
  minAvailable: 1
ports:
  http:
    containerPort: 8080
    protocol: TCP
pvc:
  payments-svc-gateway-data:
    accessModes:
    - ReadWriteOnce
    size: 30Gi
rbac:
  roleBindings:
    payments-svc-gateway-rb:
      roleRef: payments-svc-gateway-role
      subjects:
      - kind: ServiceAccount
        name: payments-svc-gateway-sa
  roles:
    payments-svc-gateway-role:
      rules:
      - apiGroups:
        - ''
        resources:
        - pods
        verbs:
        - get
        - list
readinessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10
replicaCount: 2
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi
restartPolicy: Always
revisionHistoryLimit: 10
route:
  enabled: true
  serviceName: payments-svc-gateway
  targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
routes:
  payments-svc-gateway-alt:
    enabled: true
    host: payments-svc-gateway-metrics.apps.example.com
    targetPort: http
    tls:
      insecureEdgeTerminationPolicy: Redirect
      termination: edge
schedulerName: default-scheduler
secrets:
  payments-svc-gateway-secret:
    data:
      TOKEN: YWE3YzYwMWVlYzM5NDA2NGI5M2E3NDg1ZTllMzBjYWQ=
    labels:
      app.kubernetes.io/name: payments-svc-gateway-secret
    type: Opaque
service:
  enabled: true
  labels:
    app.kubernetes.io/name: payments-svc-gateway
  ports:
    http:
      port: 80
      protocol: TCP
      targetPort: http
  type: ClusterIP
serviceAccount:
  automountServiceAccountToken: true
  create: true
  name: payments-svc-gateway-sa
services:
  payments-svc-gateway-metrics:
    enabled: true
    labels:
      app.kubernetes.io/name: payments-svc-gateway-metrics
    ports:
      http:
        port: 9090
        protocol: TCP
        targetPort: http
    selector:
      app: payments-svc-gateway
    type: ClusterIP
strategy:
  rollingUpdate:
    maxSurge: 25%
    maxUnavailable: 25%
  type: RollingUpdate
terminationGracePeriodSeconds: 30
terminationMessagePath: /dev/termination-log
terminationMessagePolicy: File
volumeMounts:
  cfg:
    mountPath: /etc/app
  data:
    mountPath: /data
volumes:
  cfg:
    configMap:
      name: payments-svc-gateway-config
  data:
    persistentVolumeClaim:
      claimName: payments-svc-gateway-data
workload:
  type: deployment

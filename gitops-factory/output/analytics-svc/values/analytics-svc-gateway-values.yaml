# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# Source: namespaces/analytics-svc/analytics-svc-gateway.yaml
# Re-run the converter to regenerate after the source manifest changes.

configMaps:
  analytics-svc-gateway-config:
    data:
      FEATURE_ALL: 'true'
    labels:
      app.kubernetes.io/name: analytics-svc-gateway-config
dnsPolicy: ClusterFirst
env:
  APP_MODE:
    value: full
envFrom:
  analytics-svc-gateway-secret:
    type: secretRef
  logging-defaults:
    type: configMapRef
hpa:
  enabled: true
  maxReplicas: 10
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 70
        type: Utilization
    type: Resource
  minReplicas: 2
image:
  pullPolicy: IfNotPresent
  repository: registry.example.com/analytics-svc-gateway
  tag: v3.2.0
livenessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 15
  periodSeconds: 20
networkPolicies:
  analytics-svc-gateway-net:
    podSelector:
      matchLabels:
        app: analytics-svc-gateway
    policyTypes:
    - Ingress
    - Egress
pdb:
  enabled: true
  minAvailable: 1
ports:
  http:
    containerPort: 8080
    protocol: TCP
pvc:
  analytics-svc-gateway-data:
    accessModes:
    - ReadWriteOnce
    size: 30Gi
rbac:
  roleBindings:
    analytics-svc-gateway-rb:
      roleRef: analytics-svc-gateway-role
      subjects:
      - kind: ServiceAccount
        name: analytics-svc-gateway-sa
  roles:
    analytics-svc-gateway-role:
      rules:
      - apiGroups:
        - ''
        resources:
        - pods
        verbs:
        - get
        - list
readinessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10
replicaCount: 2
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi
restartPolicy: Always
revisionHistoryLimit: 10
route:
  enabled: true
  targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
routes:
  analytics-svc-gateway-alt:
    enabled: true
    host: analytics-svc-gateway-metrics.apps.example.com
    serviceName: analytics-svc-gateway-metrics
    targetPort: http
    tls:
      insecureEdgeTerminationPolicy: Redirect
      termination: edge
schedulerName: default-scheduler
secrets:
  analytics-svc-gateway-secret:
    data:
      TOKEN: ZjNmY2RiMGE1NzViNDIwZDlmZWIyZTg0NTMwMzE1YjI=
    labels:
      app.kubernetes.io/name: analytics-svc-gateway-secret
    type: Opaque
service:
  enabled: true
  labels:
    app.kubernetes.io/name: analytics-svc-gateway
  ports:
    http:
      port: 80
      protocol: TCP
      targetPort: http
  type: ClusterIP
serviceAccount:
  automountServiceAccountToken: true
  create: true
  name: analytics-svc-gateway-sa
services:
  analytics-svc-gateway-metrics:
    enabled: true
    labels:
      app.kubernetes.io/name: analytics-svc-gateway-metrics
    ports:
      http:
        port: 9090
        protocol: TCP
        targetPort: http
    selector:
      app: analytics-svc-gateway
    type: ClusterIP
strategy:
  rollingUpdate:
    maxSurge: 25%
    maxUnavailable: 25%
  type: RollingUpdate
terminationGracePeriodSeconds: 30
terminationMessagePath: /dev/termination-log
terminationMessagePolicy: File
volumeMounts:
  cfg:
    mountPath: /etc/app
  data:
    mountPath: /data
volumes:
  cfg:
    configMap:
      name: analytics-svc-gateway-config
  data:
    persistentVolumeClaim:
      claimName: analytics-svc-gateway-data
workload:
  type: deployment

# Auto-generated MINIMAL override for 'platform-ops-agent-1' — image + literal env only.
# This is the client's single source of truth for day-2 tuning; safe to hand-edit
# and applied LAST (highest precedence) over platform-ops-agent-1-values.yaml.

image:
  repository: registry.example.com/platform-ops-agent-1
  tag: v3.2.0

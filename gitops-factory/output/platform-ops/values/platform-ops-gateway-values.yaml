# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# Source: namespaces/platform-ops/platform-ops-gateway.yaml
# Re-run the converter to regenerate after the source manifest changes.

configMaps:
  platform-ops-gateway-config:
    data:
      FEATURE_ALL: 'true'
    labels:
      app.kubernetes.io/name: platform-ops-gateway-config
dnsPolicy: ClusterFirst
envFrom:
  logging-defaults:
    type: configMapRef
  platform-ops-gateway-secret:
    type: secretRef
hpa:
  enabled: true
  maxReplicas: 10
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 70
        type: Utilization
    type: Resource
  minReplicas: 2
image:
  pullPolicy: IfNotPresent
  repository: registry.example.com/platform-ops-gateway
livenessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 15
  periodSeconds: 20
networkPolicies:
  platform-ops-gateway-net:
    podSelector:
      matchLabels:
        app: platform-ops-gateway
    policyTypes:
    - Ingress
    - Egress
pdb:
  enabled: true
  minAvailable: 1
ports:
  http:
    containerPort: 8080
    protocol: TCP
pvc:
  platform-ops-gateway-data:
    accessModes:
    - ReadWriteOnce
    size: 30Gi
rbac:
  roleBindings:
    platform-ops-gateway-rb:
      roleRef: platform-ops-gateway-role
      subjects:
      - kind: ServiceAccount
        name: platform-ops-gateway-sa
  roles:
    platform-ops-gateway-role:
      rules:
      - apiGroups:
        - ''
        resources:
        - pods
        verbs:
        - get
        - list
readinessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10
replicaCount: 2
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi
restartPolicy: Always
revisionHistoryLimit: 10
route:
  enabled: true
  targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
routes:
  platform-ops-gateway-alt:
    enabled: true
    host: platform-ops-gateway-metrics.apps.example.com
    serviceName: platform-ops-gateway-metrics
    targetPort: http
    tls:
      insecureEdgeTerminationPolicy: Redirect
      termination: edge
schedulerName: default-scheduler
secrets:
  platform-ops-gateway-secret:
    data:
      TOKEN: ZTk0ODY4MjQ2MGQwNDQ3NmJhMDI0NzEwYjgwMzhkMTY=
    labels:
      app.kubernetes.io/name: platform-ops-gateway-secret
    type: Opaque
service:
  enabled: true
  labels:
    app.kubernetes.io/name: platform-ops-gateway
  ports:
    http:
      port: 80
      protocol: TCP
      targetPort: http
  type: ClusterIP
serviceAccount:
  automountServiceAccountToken: true
  create: true
  name: platform-ops-gateway-sa
services:
  platform-ops-gateway-metrics:
    enabled: true
    labels:
      app.kubernetes.io/name: platform-ops-gateway-metrics
    ports:
      http:
        port: 9090
        protocol: TCP
        targetPort: http
    selector:
      app: platform-ops-gateway
    type: ClusterIP
strategy:
  rollingUpdate:
    maxSurge: 25%
    maxUnavailable: 25%
  type: RollingUpdate
terminationGracePeriodSeconds: 30
terminationMessagePath: /dev/termination-log
terminationMessagePolicy: File
volumeMounts:
  cfg:
    mountPath: /etc/app
  data:
    mountPath: /data
volumes:
  cfg:
    configMap:
      name: platform-ops-gateway-config
  data:
    persistentVolumeClaim:
      claimName: platform-ops-gateway-data
workload:
  type: deployment

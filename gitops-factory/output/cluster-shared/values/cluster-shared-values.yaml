# Auto-generated by convert_to_universal_chart.py
# Cluster-scoped resources deduplicated across the ENTIRE run — deploy exactly ONCE.
workload:
  type: none
service:
  enabled: false

clusterSecretStores: {}
persistentVolumes: {}
rbac:
  clusterRoleBindings: {}
  clusterRoles:
    cross-namespace-log-reader:
      rules:
      - apiGroups:
        - ''
        resources:
        - pods
        - pods/log
        - events
        verbs:
        - get
        - list
        - watch
scc:
  restricted-custom-scc:
    allowPrivilegeEscalation: false
    allowPrivilegedContainer: false
    fsGroup:
      type: MustRunAs
    requiredDropCapabilities:
    - ALL
    runAsUser:
      type: MustRunAsRange
    seLinuxContext:
      type: MustRunAs
    supplementalGroups:
      type: RunAsAny
    users:
    - system:serviceaccount:platform-shared:scc-user
    volumes:
    - configMap
    - downwardAPI
    - emptyDir
    - persistentVolumeClaim
    - projected
    - secret
storageClasses:
  fast-ssd:
    allowVolumeExpansion: true
    parameters:
      encrypted: 'true'
      type: gp3
    provisioner: ebs.csi.aws.com
    reclaimPolicy: Delete
    volumeBindingMode: WaitForFirstConsumer

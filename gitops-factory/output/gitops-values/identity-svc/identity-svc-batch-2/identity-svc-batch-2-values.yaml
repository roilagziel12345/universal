# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# Source: namespaces/identity-svc/identity-svc-batch-2.yaml
# Re-run the converter to regenerate after the source manifest changes.

cronjobs:
  identity-svc-batch-2-backup:
    concurrencyPolicy: Forbid
    failedJobsHistoryLimit: 1
    jobTemplate:
      backoffLimit: 2
      command:
      - /bin/sh
      - -c
      - pg_dump $DATABASE_URL | gzip > /backup/$(date +%Y%m%d).sql.gz
      envFrom:
        identity-svc-batch-2-db-secret:
          type: secretRef
      image:
        repository: registry.example.com/identity-svc-batch-2-backup
        tag: 2.3.1
      resources:
        limits:
          cpu: 500m
          memory: 512Mi
        requests:
          cpu: 100m
          memory: 128Mi
      restartPolicy: OnFailure
    schedule: 0 2 * * *
    successfulJobsHistoryLimit: 3
  identity-svc-batch-2-cleanup:
    concurrencyPolicy: Forbid
    failedJobsHistoryLimit: 1
    jobTemplate:
      backoffLimit: 2
      command:
      - python
      - manage.py
      - clearsessions
      image:
        repository: registry.example.com/identity-svc-batch-2-cleanup
        tag: v3.2.0
      resources:
        limits:
          cpu: 500m
          memory: 512Mi
        requests:
          cpu: 100m
          memory: 128Mi
      restartPolicy: OnFailure
    schedule: '*/15 * * * *'
    successfulJobsHistoryLimit: 3
jobs:
  identity-svc-batch-2-backup-29384756:
    backoffLimit: 3
    command:
    - /bin/sh
    - -c
    - pg_dump $DATABASE_URL | gzip > /backup/$(date +%Y%m%d).sql.gz
    envFrom:
      identity-svc-batch-2-db-secret:
        type: secretRef
    image:
      repository: registry.example.com/identity-svc-batch-2-backup
      tag: 2.3.1
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 100m
        memory: 128Mi
    restartPolicy: Never
  identity-svc-batch-2-migrate:
    backoffLimit: 3
    command:
    - python
    - manage.py
    - migrate
    image:
      repository: registry.example.com/identity-svc-batch-2-migrate
      tag: 1.0.0
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 100m
        memory: 128Mi
    restartPolicy: Never
pvc:
  identity-svc-batch-2-backups:
    accessModes:
    - ReadWriteOnce
    size: 100Gi
secrets:
  identity-svc-batch-2-db-secret:
    data:
      DATABASE_URL: cG9zdGdyZXM6Ly91c2VyOnBhc3NAaWRlbnRpdHktc3ZjLWJhdGNoLTItZGI6NTQzMi9hcHA=
    labels:
      app.kubernetes.io/name: identity-svc-batch-2-db-secret
    type: Opaque
service:
  enabled: false
workload:
  type: none

# Auto-generated MINIMAL override for 'identity-svc-worker-a' — image + literal env only.
# This is the client's single source of truth for day-2 tuning; safe to hand-edit
# and applied LAST (highest precedence) over identity-svc-worker-a-values.yaml.

image:
  repository: registry.example.com/identity-svc-worker-a
  tag: v3.2.0

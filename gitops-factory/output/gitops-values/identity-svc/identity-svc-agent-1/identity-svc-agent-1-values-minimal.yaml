# Auto-generated MINIMAL override for 'identity-svc-agent-1' — image + literal env only.
# This is the client's single source of truth for day-2 tuning; safe to hand-edit
# and applied LAST (highest precedence) over identity-svc-agent-1-values.yaml.

image:
  repository: registry.example.com/identity-svc-agent-1
  tag: 1.0.0

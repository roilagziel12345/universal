# Auto-generated MINIMAL override for 'payments-svc-gateway' — image + literal env only.
# This is the client's single source of truth for day-2 tuning; safe to hand-edit
# and applied LAST (highest precedence) over payments-svc-gateway-values.yaml.

env:
  APP_MODE:
    value: full
image:
  repository: registry.example.com/payments-svc-gateway
  tag: latest

# Auto-generated MINIMAL override for 'payments-svc-worker-a' — image + literal env only.
# This is the client's single source of truth for day-2 tuning; safe to hand-edit
# and applied LAST (highest precedence) over payments-svc-worker-a-values.yaml.

image:
  repository: registry.example.com/payments-svc-worker-a
  tag: v3.2.0

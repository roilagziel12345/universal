# Auto-generated MINIMAL override for 'analytics-svc-checkout' — image + literal env only.
# This is the client's single source of truth for day-2 tuning; safe to hand-edit
# and applied LAST (highest precedence) over analytics-svc-checkout-values.yaml.

env:
  APP_ENV:
    value: production
image:
  repository: registry.example.com/analytics-svc-checkout
  tag: 2.3.1

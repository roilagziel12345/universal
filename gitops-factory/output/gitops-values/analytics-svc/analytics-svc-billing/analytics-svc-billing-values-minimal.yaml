# Auto-generated MINIMAL override for 'analytics-svc-billing' — image + literal env only.
# This is the client's single source of truth for day-2 tuning; safe to hand-edit
# and applied LAST (highest precedence) over analytics-svc-billing-values.yaml.

env:
  APP_ENV:
    value: production
image:
  repository: registry.example.com/analytics-svc-billing
  tag: 1.0.0

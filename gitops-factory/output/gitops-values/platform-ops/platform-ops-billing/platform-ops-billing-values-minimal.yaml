# Auto-generated MINIMAL override for 'platform-ops-billing' — image + literal env only.
# This is the client's single source of truth for day-2 tuning; safe to hand-edit
# and applied LAST (highest precedence) over platform-ops-billing-values.yaml.

env:
  APP_ENV:
    value: production
image:
  repository: registry.example.com/platform-ops-billing
  tag: latest

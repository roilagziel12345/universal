# Auto-generated by convert_to_universal_chart.py — DO NOT EDIT BY HAND.
# Source: namespaces/platform-ops/platform-ops-edge-3.yaml
# Re-run the converter to regenerate after the source manifest changes.

dnsPolicy: ClusterFirst
image:
  pullPolicy: IfNotPresent
  repository: registry.example.com/platform-ops-edge-3
  tag: 1.0.0
livenessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 15
  periodSeconds: 20
networkPolicies:
  platform-ops-edge-3-allow-ingress:
    ingress:
    - from:
      - podSelector:
          matchLabels:
            tier: frontend
      ports:
      - port: 8080
        protocol: TCP
    podSelector:
      matchLabels:
        app: platform-ops-edge-3
    policyTypes:
    - Ingress
  platform-ops-edge-3-deny-egress-external:
    egress:
    - ports:
      - port: 53
        protocol: UDP
    podSelector:
      matchLabels:
        app: platform-ops-edge-3
    policyTypes:
    - Egress
ports:
  http:
    containerPort: 8080
    protocol: TCP
readinessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10
replicaCount: 2
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi
restartPolicy: Always
revisionHistoryLimit: 10
route:
  enabled: true
  host: platform-ops-edge-3.apps.example.com
  serviceName: platform-ops-edge-3
  targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
routes:
  platform-ops-edge-3-admin-console:
    enabled: true
    host: platform-ops-edge-3-admin.apps.example.com
    targetPort: http
    tls:
      insecureEdgeTerminationPolicy: Redirect
      termination: edge
schedulerName: default-scheduler
service:
  enabled: true
  labels:
    app.kubernetes.io/name: platform-ops-edge-3
  ports:
    http:
      port: 80
      protocol: TCP
      targetPort: http
  type: ClusterIP
services:
  platform-ops-edge-3-admin:
    enabled: true
    labels:
      app.kubernetes.io/name: platform-ops-edge-3-admin
    ports:
      http:
        port: 8081
        protocol: TCP
        targetPort: http
    selector:
      app: platform-ops-edge-3
    type: ClusterIP
strategy:
  rollingUpdate:
    maxSurge: 25%
    maxUnavailable: 25%
  type: RollingUpdate
terminationGracePeriodSeconds: 30
terminationMessagePath: /dev/termination-log
terminationMessagePolicy: File
workload:
  type: deployment

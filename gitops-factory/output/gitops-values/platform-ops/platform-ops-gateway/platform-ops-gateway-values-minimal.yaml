# Auto-generated MINIMAL override for 'platform-ops-gateway' — image + literal env only.
# This is the client's single source of truth for day-2 tuning; safe to hand-edit
# and applied LAST (highest precedence) over platform-ops-gateway-values.yaml.

env:
  APP_MODE:
    value: full
image:
  repository: registry.example.com/platform-ops-gateway
  tag: latest
